<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ShoeStop - Admin Dashboard</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <!-- Custom CSS -->
    <style>
        :root {
            --topbar-bg: #f8f9fa;
            --topbar-height: 56px;
            --sidebar-bg: rgba(0, 0, 0, 0.926);
            --sidebar-color: #e1e1e1;
            --sidebar-active-bg: rgba(255, 255, 255, 0.1);
            --sidebar-hover-bg: rgba(255, 255, 255, 0.05);
            --sidebar-width: 200px;
            --sidebar-collapsed-width: 80px;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow-x: hidden;
        }
        
        /* Sidebar styling */
        
        #sidebar .sidebar-header {
            padding: 8px;
            background: rgba(0, 0, 0, 0.2);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            animation: blinkColors 30s infinite;
        }

        @keyframes blinkColors {
            0%   { color: #ff3860; }
            20%  { color: #ffdd57; }
            40%  { color: #9bff5c; }
            60%  { color: #ff6cfd; }
            80%  { color: #5761f3; }
            100% { color: #ff3860; }
        }
        
        /* Topbar styling */
        #topbar {
            height: var(--topbar-height);
            background: var(--topbar-bg);
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            position: fixed;
            left: var(--sidebar-width);
            right: 0;
            z-index: 999;
        }
        
        /* Main content area */
        #content {
            margin-left: var(--sidebar-width);
            padding: 20px;
            padding-top: calc(var(--topbar-height) + 20px);
            transition: all 0.3s;
        }
        
        /* Card styling */
        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }
        
        .card-header {
            background: #fff;
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
            font-weight: 600;
        }
        
        /* Responsive adjustments */
        @media (max-width: 768px) {
            #content {
                margin-left: 0;
            }
            
            #topbar {
                left: 0;
            }
        }

    .sidebar {
        width: var(--sidebar-width);
        height: 100vh;
        background: var(--sidebar-bg);
        color: var(--sidebar-color);
        position: fixed;
        transition: all 0.3s;
        z-index: 1000;
        display: flex;
        flex-direction: column;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
    }

    .sidebar-menu {
        flex: 1;
        padding: 4px;
    }
    .components li {
        font-size: 14px;
    }

    .components li a {
        padding: 4px 4px;
        color: var(--sidebar-color);
        display: block;
        text-decoration: none;
        transition: all 0.3s;
        border-left: 3px solid transparent;
    }

    .components li a:hover {
        background: var(--sidebar-hover-bg);
        color: #fff;
    }

    .components li.active > a {
        background: var(--sidebar-active-bg);
        color: #fff;
        border-left: 2px solid #4e73df;
    }

    .components li a i {
        min-width: 20px;
        text-align: center;
    }

    .submenu li a {
        padding: 10px 20px 10px 50px;
        font-size: 0.9em;
    }

    .submenu li a:hover {
        background: var(--sidebar-hover-bg);
    }

    .menu-divider {
        padding: 10px 20px;
        color: rgba(255, 255, 255, 0.3);
        font-size: 0.75em;
        letter-spacing: 1px;
    }

    /* Collapsed state */
    .sidebar.collapsed {
        width: var(--sidebar-collapsed-width);
    }

    @media (max-width: 992px) {
        .sidebar {
            margin-left: -var(--sidebar-width);
        }
        
        .sidebar.active {
            margin-left: 0;
        }
    }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <nav id="sidebar" class="sidebar">
        <div class="sidebar-header">
            <div class="d-flex align-items-center justify-content-between">
                <h2 class="mb-0">Eliteklicks</h2>
            </div>
        </div>
        
        <div class="sidebar-menu">
            <ul class="list-unstyled components">
                <li class="active">
                    <a href="/admin" class="d-flex align-items-center">
                        <i class="bi bi-speedometer2 me-3"></i>
                        <span>Dashboard</span>
                    </a>
                </li>
                <hr class="m-1 p-0">
                <li>
                    <a href="/admin/add_product">
                        <i class="bi bi-plus-circle me-3"></i>
                        <span>Add Product</span>
                    </a>
                    <a href="/admin/product_list">
                        <i class="bi bi-list-ul me-3"></i>
                        <span>Products List</span>
                    </a>
                </li>
                <hr class="m-1 p-0">
                <li>
                    <a href="/admin/pending_orders" class="d-flex align-items-center">
                        <i class="bi bi-hourglass-split me-3"></i>
                        <span>Pending Orders</span>
                    </a>
                    <a href="/admin/dispatch_orders" class="d-flex align-items-center">
                        <i class="bi bi-truck me-3"></i>
                        <span>Dispatch Orders</span>
                    </a>
                    <a href="/admin/delivered_orders" class="d-flex align-items-center">
                        <i class="bi bi-check-circle me-3"></i>
                        <span>Delivered Orders</span>
                    </a>
                    <a href="/admin/cancelled_orders" class="d-flex align-items-center">
                        <i class="bi bi-x-circle me-3"></i>
                        <span>Cancelled Orders</span>
                    </a>
                    <a href="/admin/return_orders" class="d-flex align-items-center">
                        <i class="bi bi-arrow-return-left me-3"></i>
                        <span>Return Orders</span>
                    </a>
                </li>
                <hr class="m-1 p-0">
                <li>
                    <a href="/admin/promotional_banners" class="d-flex align-items-center">
                        <i class="bi bi-image me-3"></i>
                        <span>Promotional Banner</span>
                    </a>
                    <a href="/admin/slider_images" class="d-flex align-items-center">
                        <i class="bi bi-image me-3"></i>
                        <span>Slider Images</span>
                    </a>
                    <a href="/admin/product_brand" class="d-flex align-items-center">
                        <i class="bi bi-shop me-3"></i>
                        <span>Product Brand</span>
                    </a>
                    <a href="/admin/product_styles" class="d-flex align-items-center">
                        <i class="bi bi-palette me-3"></i>
                        <span>Product Styles</span>
                    </a>
                    <a href="/admin/product_types" class="d-flex align-items-center">
                        <i class="bi bi-list-ul me-3"></i>
                        <span>Product Types</span>
                    </a>
                </li>
            </ul>
        </div>
    </nav>

    <!-- Topbar -->
    <nav id="topbar" class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <div class="d-flex align-items-center">
                <form class="d-flex" role="search" style="min-width: 280px;">
                    <div class="input-group">
                        <input class="form-control" type="search" placeholder="Search dashboard..." aria-label="Search" style="border-right: 0;">
                        <button class="btn btn-outline-primary" type="submit" style="border-left: 0;">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                </form>
            </div>            
            <div class="d-flex align-items-center ms-auto">
                <div class="dropdown">
                    <button class="btn dropdown-toggle" style="background-color: rgb(88, 180, 88); color: white; border: none;" type="button" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-person-circle me-1"></i>
                        Admin User
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                        <li><a class="dropdown-item" href="#"><i class="bi bi-person me-2"></i>Profile</a></li>
                        <li><a class="dropdown-item" href="#"><i class="bi bi-gear me-2"></i>Settings</a></li>
                        <li><a class="dropdown-item" href="#"><i class="bi bi-question-circle me-2"></i>Help</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="/admin/logout"><i class="bi bi-box-arrow-right me-2"></i>Logout</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>